const API='php/';
async function req(file,opts={}){const r=await fetch(API+file,{credentials:'same-origin',cache:'no-store',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok||d.ok===false)throw new Error(d.error||'Operação não concluída');return d}
function toast(msg){const t=document.createElement('div');t.className='toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2800)}
function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
async function ensureUser(){const d=await req('auth.php?action=me');if(!d.user){location.href='index.html';return null}const el=document.querySelector('#userName'); if(el) el.textContent=d.user.name;document.querySelectorAll('.admin-link').forEach(a=>a.classList.toggle('hidden',d.user.role!=='ADMIN'));return d.user}
function setupCommon(){document.querySelectorAll('[data-logout]').forEach(b=>b.onclick=async()=>{await req('auth.php?action=logout');location.href='index.html'});document.querySelectorAll('[data-toggle-password]').forEach(b=>b.onclick=()=>{const i=b.parentElement.querySelector('input');i.type=i.type==='password'?'text':'password'});document.querySelectorAll('[data-role-login]').forEach(a=>a.onclick=e=>{e.preventDefault();setLoginRole(a.dataset.roleLogin||'USER');document.querySelector('#login')?.scrollIntoView({behavior:'smooth'})})}
function setLoginRole(role){role=role==='ADMIN'?'ADMIN':'USER';const input=document.querySelector('#loginRole');const context=document.querySelector('#loginContext');const msg=document.querySelector('#loginMsg');if(input)input.value=role;if(context)context.textContent=role==='ADMIN'?'Área do Administrador':'Área do usuário';if(msg)msg.textContent='';const f=document.querySelector('#loginForm');if(f)f.dataset.role=role}
async function initHome(){setupCommon();const f=document.querySelector('#loginForm');if(!f)return;const params=new URLSearchParams(location.search);setLoginRole(params.get('role')==='ADMIN'?'ADMIN':'USER');f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f);const msg=document.querySelector('#loginMsg');try{const d=await req('auth.php?action=login',{method:'POST',body:JSON.stringify(Object.fromEntries(fd))});toast('Login efetuado');location.href=d.user.role==='ADMIN'?'admin.html':'dashboard.html'}catch(err){msg.textContent=err.message}}}
async function initDashboard(){const u=await ensureUser();if(!u)return;const d=await req('dashboard.php');const c=document.querySelector('#pageContent');c.innerHTML=`<div class="cards"><div class="card metric">Salas ativas<strong>${d.metrics.rooms}</strong></div><div class="card metric">Equipamentos<strong>${d.metrics.equipment}</strong></div><div class="card metric">Minhas reservas<strong>${d.metrics.my_reservations}</strong></div><div class="card metric">Utilizadores<strong>${d.metrics.users}</strong></div></div><div class="panel" style="margin-top:20px"><h2>Próximas reservas</h2><div class="table-wrap"><table class="table"><thead><tr><th>Recurso</th><th>Início</th><th>Fim</th><th>Finalidade</th></tr></thead><tbody>${d.upcoming.map(r=>`<tr><td>${esc(r.room_name||r.equipment_name)}</td><td>${esc(r.start_at)}</td><td>${esc(r.end_at)}</td><td>${esc(r.purpose)}</td></tr>`).join('')||'<tr><td colspan="4">Nenhuma reserva futura.</td></tr>'}</tbody></table></div></div>`}
function modal(title,body){const m=document.createElement('div');m.className='modal';m.innerHTML=`<div class="modal-box"><div class="modal-head"><h2>${title}</h2><button class="close">×</button></div>${body}</div>`;document.body.append(m);m.querySelector('.close').onclick=()=>m.remove();return m}
async function initRooms(){const u=await ensureUser();if(!u)return;const c=document.querySelector('#pageContent');const d=await req('rooms.php');const render=()=>{c.innerHTML=`<div class="actions" style="margin-bottom:14px">${u.role==='ADMIN'?'<button class="btn primary" id="new">Nova sala</button>':''}</div><div class="table-wrap"><table class="table"><thead><tr><th>Número</th><th>Nome</th><th>Capacidade</th><th>Localização</th><th>Recursos</th><th></th></tr></thead><tbody>${d.items.map(r=>`<tr><td>${esc(r.number)}</td><td>${esc(r.name)}</td><td>${r.capacity}</td><td>${esc(r.location)}</td><td>${esc(r.resources)}</td><td>${u.role==='ADMIN'?`<button class="btn" data-edit="${r.id}">Editar</button> <button class="btn danger" data-del="${r.id}">Inativar</button>`:''}</td></tr>`).join('')}</tbody></table></div>`;if(u.role==='ADMIN')c.querySelector('#new').onclick=()=>roomForm();c.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>roomForm(d.items.find(x=>x.id==b.dataset.edit)));c.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(confirm('Inativar esta sala?')){await req('rooms.php',{method:'POST',body:JSON.stringify({action:'delete',id:b.dataset.del})});toast('Sala inativada');initRooms()}})};function roomForm(r={}){const m=modal(r.id?'Editar sala':'Nova sala',`<form id="rf"><div class="form-grid"><label class="field"><span>Número</span><input name="number" value="${esc(r.number)}" required></label><label class="field"><span>Nome</span><input name="name" value="${esc(r.name)}" required></label><label class="field"><span>Capacidade</span><input name="capacity" type="number" min="1" value="${r.capacity||1}" required></label><label class="field"><span>Localização</span><input name="location" value="${esc(r.location)}" required></label><label class="field full"><span>Recursos</span><textarea name="resources">${esc(r.resources)}</textarea></label></div><button class="btn primary">Salvar</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));x.id=r.id||0;await req('rooms.php',{method:'POST',body:JSON.stringify(x)});m.remove();toast('Sala salva');initRooms()}}render()}
async function initEquipment(){const u=await ensureUser();if(!u)return;const c=document.querySelector('#pageContent');const d=await req('equipment.php');const render=()=>{c.innerHTML=`<div class="actions" style="margin-bottom:14px">${u.role==='ADMIN'?'<button class="btn primary" id="new">Novo equipamento</button>':''}</div><div class="table-wrap"><table class="table"><thead><tr><th>Código</th><th>Nome</th><th>Descrição</th><th>Local</th><th></th></tr></thead><tbody>${d.items.map(r=>`<tr><td>${esc(r.code)}</td><td>${esc(r.name)}</td><td>${esc(r.description)}</td><td>${esc(r.location)}</td><td>${u.role==='ADMIN'?`<button class="btn" data-edit="${r.id}">Editar</button> <button class="btn danger" data-del="${r.id}">Inativar</button>`:''}</td></tr>`).join('')||'<tr><td colspan="5">Nenhum equipamento encontrado.</td></tr>'}</tbody></table></div>`;if(u.role==='ADMIN')c.querySelector('#new').onclick=()=>form();c.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>form(d.items.find(x=>x.id==b.dataset.edit)));c.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(confirm('Inativar equipamento?')){await req('equipment.php',{method:'POST',body:JSON.stringify({action:'delete',id:b.dataset.del})});toast('Equipamento inativado');initEquipment()}})};function form(r={}){const m=modal(r.id?'Editar equipamento':'Novo equipamento',`<form><div class="form-grid"><label class="field"><span>Código</span><input name="code" value="${esc(r.code||'')}" required></label><label class="field"><span>Nome</span><input name="name" value="${esc(r.name||'')}" required></label><label class="field full"><span>Descrição</span><textarea name="description" required>${esc(r.description||'')}</textarea></label><label class="field"><span>Localização</span><input name="location" value="${esc(r.location||'')}"></label></div><button class="btn primary">Salvar</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));x.id=r.id||0;await req('equipment.php',{method:'POST',body:JSON.stringify(x)});m.remove();toast('Equipamento salvo');initEquipment()}}render()}
async function initReservations(){const u=await ensureUser();if(!u)return;const [rooms,equip,res]=await Promise.all([req('rooms.php'),req('equipment.php'),req('reservations.php')]);const c=document.querySelector('#pageContent');c.innerHTML=`<div class="actions" style="margin-bottom:14px"><button class="btn primary" id="new">Nova reserva</button></div><div class="table-wrap"><table class="table"><thead><tr><th>Recurso</th><th>Início</th><th>Fim</th><th>Finalidade</th><th>Status</th><th></th></tr></thead><tbody>${res.items.map(r=>`<tr><td>${esc(r.room_name||r.equipment_name)}</td><td>${esc(r.start_at)}</td><td>${esc(r.end_at)}</td><td>${esc(r.purpose)}</td><td>${esc(r.status)}</td><td class="actions">${r.status==='ACTIVE'?`<button class="btn danger" data-cancel="${r.id}">Cancelar</button><button class="btn" data-report="${r.id}" data-resource="${esc(r.room_name||r.equipment_name)}">Reportar</button>`:''}</td></tr>`).join('')||'<tr><td colspan="6">Nenhuma reserva encontrada.</td></tr>'}</tbody></table></div>`;c.querySelector('#new').onclick=()=>reservationForm();c.querySelectorAll('[data-cancel]').forEach(b=>b.onclick=async()=>{if(confirm('Cancelar esta reserva?')){try{await req('reservations.php',{method:'POST',body:JSON.stringify({action:'cancel',id:b.dataset.cancel})});toast('Reserva cancelada');initReservations()}catch(e){alert(e.message)}}});c.querySelectorAll('[data-report]').forEach(b=>b.onclick=()=>reportForm(b.dataset.report,b.dataset.resource));function reportForm(reservationId,resource){const m=modal('Reportar',`<form><div class="panel" style="margin-bottom:14px"><strong>Reserva vinculada</strong><p style="margin:6px 0 0">${esc(resource)}</p></div><label class="field"><span>Relato</span><textarea name="message" placeholder="Descreva o que deseja comunicar sobre esta reserva." required></textarea></label><button class="btn primary">Enviar reporte</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await req('observations.php',{method:'POST',body:JSON.stringify({action:'save',reservation_id:Number(reservationId),message:e.target.message.value})});m.remove();toast('Reporte enviado')}catch(err){alert(err.message)}}}function reservationForm(){const m=modal('Nova reserva',`<form><div class="form-grid"><label class="field"><span>Tipo</span><select name="type" id="rtype"><option value="room">Sala</option><option value="equipment">Equipamento</option></select></label><label class="field" id="resourceField"><span>Recurso</span><select name="room_id" id="resource">${rooms.items.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.number)} — ${esc(x.name)}</option>`).join('')}</select></label><label class="field"><span>Data</span><input type="date" name="reservation_date" required></label><label class="field"><span>Horário de início</span><input type="text" name="start_time" placeholder="HH:MM" inputmode="numeric" maxlength="5" required></label><label class="field"><span>Horário de término</span><input type="text" name="end_time" placeholder="HH:MM" inputmode="numeric" maxlength="5" required></label><label class="field full"><span>Finalidade</span><textarea name="purpose" required></textarea></label></div><p class="hint">Digite os horários no formato 24 horas, por exemplo: 08:30 às 10:15.</p><button class="btn primary">Reservar</button></form>`);const type=m.querySelector('#rtype'),field=m.querySelector('#resourceField');type.onchange=()=>{if(type.value==='room'){field.innerHTML='<span>Recurso</span><select name="room_id" id="resource">'+rooms.items.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.number)} — ${esc(x.name)}</option>`).join('')+'</select>'}else{field.innerHTML='<span>Recurso</span><select name="equipment_id" id="resource">'+equip.items.filter(x=>x.active).map(x=>`<option value="${x.id}">${esc(x.code)} — ${esc(x.name)}</option>`).join('')+'</select>'}};m.querySelectorAll('input[name="start_time"],input[name="end_time"]').forEach(inp=>inp.addEventListener('input',()=>{let v=inp.value.replace(/\D/g,'').slice(0,4);if(v.length>2)v=v.slice(0,2)+':'+v.slice(2);inp.value=v}));m.querySelector('form').onsubmit=async e=>{e.preventDefault();const x=Object.fromEntries(new FormData(e.target));delete x.type;const date=x.reservation_date,startTime=x.start_time,endTime=x.end_time;if(!/^\d{4}-\d{2}-\d{2}$/.test(date)||!/^([01]\d|2[0-3]):[0-5]\d$/.test(startTime)||!/^([01]\d|2[0-3]):[0-5]\d$/.test(endTime)){alert('Informe a data e os horários no formato HH:MM.');return}x.start_at=`${date} ${startTime}:00`;x.end_at=`${date} ${endTime}:00`;delete x.reservation_date;delete x.start_time;delete x.end_time;try{await req('reservations.php',{method:'POST',body:JSON.stringify(x)});m.remove();toast('Reserva criada');initReservations()}catch(err){alert(err.message)}}}}
async function initObservations(){const u=await ensureUser();if(!u)return;const d=await req('observations.php');const c=document.querySelector('#pageContent');c.innerHTML=`<div class="page-head"><div><p class="eyebrow">COMUNICAÇÕES VINCULADAS ÀS RESERVAS</p><h1>Reportes</h1></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Data</th><th>Utilizador</th><th>Reserva</th><th>Recurso</th><th>Relato</th><th>Status</th><th></th></tr></thead><tbody>${d.items.map(x=>`<tr><td>${esc(x.created_at)}</td><td>${esc(x.user_name)}</td><td>#${esc(x.reservation_id||'—')}</td><td>${esc(x.room_name||x.equipment_name||'—')}</td><td>${esc(x.message)}</td><td>${esc(x.status)}</td><td>${x.status==='OPEN'?(u.role==='ADMIN'?`<button class="btn" data-close="${x.id}">Encerrar</button>`:`<button class="btn danger" data-del="${x.id}">Excluir</button>`):''}</td></tr>`).join('')||'<tr><td colspan="7">Nenhum reporte encontrado.</td></tr>'}</tbody></table></div>`;c.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{await req('observations.php',{method:'POST',body:JSON.stringify({action:'delete',id:b.dataset.del})});initObservations()});c.querySelectorAll('[data-close]').forEach(b=>b.onclick=async()=>{await req('observations.php',{method:'POST',body:JSON.stringify({action:'close',id:b.dataset.close})});initObservations()})}
async function initAdmin(){const u=await ensureUser();if(!u||u.role!=='ADMIN'){location.href='dashboard.html';return}const d=await req('users.php');const c=document.querySelector('#pageContent');c.innerHTML=`<div class="actions" style="margin-bottom:14px"><button class="btn primary" id="new">Novo usuário</button></div><div class="table-wrap"><table class="table"><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th>Ativo</th><th></th></tr></thead><tbody>${d.items.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.email)}</td><td>${x.role}</td><td>${x.active?'Sim':'Não'}</td><td><button class="btn" data-edit="${x.id}">Editar</button> <button class="btn danger" data-del="${x.id}">Inativar</button></td></tr>`).join('')}</tbody></table></div>`;c.querySelector('#new').onclick=()=>form();c.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>form(d.items.find(x=>x.id==b.dataset.edit)));c.querySelectorAll('[data-del]').forEach(b=>b.onclick=async()=>{if(confirm('Inativar usuário?')){await req('users.php',{method:'POST',body:JSON.stringify({action:'delete',id:b.dataset.del})});initAdmin()}});function form(x={}){const m=modal(x.id?'Editar usuário':'Novo usuário',`<form><div class="form-grid"><label class="field"><span>Nome</span><input name="name" value="${esc(x.name)}" required></label><label class="field"><span>E-mail</span><input name="email" type="email" value="${esc(x.email)}" required></label><label class="field"><span>Perfil</span><select name="role"><option ${x.role==='USER'?'selected':''}>USER</option><option ${x.role==='ADMIN'?'selected':''}>ADMIN</option></select></label><label class="field"><span>Senha ${x.id?'(opcional)':''}</span><input name="password" type="password"></label><label class="field"><span>Ativo</span><select name="active"><option value="1" ${x.active?'selected':''}>Sim</option><option value="0" ${!x.active?'selected':''}>Não</option></select></label></div><button class="btn primary">Salvar</button></form>`);m.querySelector('form').onsubmit=async e=>{e.preventDefault();const z=Object.fromEntries(new FormData(e.target));z.id=x.id||0;z.active=z.active==='1';await req('users.php',{method:'POST',body:JSON.stringify(z)});m.remove();toast('Usuário salvo');initAdmin()}}}
async function initReports(){const u=await ensureUser();if(!u||u.role!=='ADMIN'){location.href='dashboard.html';return}const d=await req('reports.php');document.querySelector('#pageContent').innerHTML=`<div class="grid"><div class="panel"><h2>Reservas por recurso</h2><table class="table"><thead><tr><th>Recurso</th><th>Total</th></tr></thead><tbody>${d.by_resource.map(x=>`<tr><td>${esc(x.resource)}</td><td>${x.total}</td></tr>`).join('')}</tbody></table></div><div class="panel"><h2>Reservas por status</h2><table class="table"><thead><tr><th>Status</th><th>Total</th></tr></thead><tbody>${d.by_status.map(x=>`<tr><td>${esc(x.status)}</td><td>${x.total}</td></tr>`).join('')}</tbody></table></div></div>`}
async function initAudit(){const u=await ensureUser();if(!u||u.role!=='ADMIN'){location.href='dashboard.html';return}const d=await req('audit.php');document.querySelector('#pageContent').innerHTML=`<div class="table-wrap"><table class="table"><thead><tr><th>Data</th><th>Usuário</th><th>Ação</th><th>Entidade</th><th>ID</th><th>Detalhes</th></tr></thead><tbody>${d.items.map(x=>`<tr><td>${esc(x.created_at)}</td><td>${esc(x.user_name||'Sistema')}</td><td>${esc(x.action)}</td><td>${esc(x.entity)}</td><td>${esc(x.entity_id)}</td><td>${esc(x.details)}</td></tr>`).join('')}</tbody></table></div>`}
setupCommon();
const path=location.pathname.split('/').pop();
(async()=>{try{if(path==='index.html'||path==='')await initHome();else if(path==='dashboard.html')await initDashboard();else if(path==='salas.html')await initRooms();else if(path==='equipamentos.html')await initEquipment();else if(path==='reservas.html')await initReservations();else if(path==='observacoes.html')await initObservations();else if(path==='admin.html')await initAdmin();else if(path==='relatorios.html')await initReports();else if(path==='auditoria.html')await initAudit()}catch(e){console.error(e);if(document.querySelector('#pageContent'))document.querySelector('#pageContent').innerHTML='<div class="panel"><strong>Erro:</strong> '+esc(e.message)+'</div>'}})();
